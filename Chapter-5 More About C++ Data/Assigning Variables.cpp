#include <iostream>
int main()
{
    unsigned int This, That, Those;

    //assign the same values to three variables
    This = That = Those = 62440;

    //Display three unsigned ints
    std::cout << This;
    std::cout << ' ';
    std::cout << That;
    std::cout << ' ';
    std::cout << Those;

    return 0;
}